# git
git related

```
[user]
	name = postalc
	email = postalzc@gmail.com
[core]
	autocrlf = false
[push]
	default = simple
[alias]
	logs = log --pretty=oneline
	last = log -1 HEAD
	last2 = log -2 HEAD
	co = checkout
	br = branch
	cm = commit
	stat = status
	dif2 = log -p -2
	dif1 = log -p -1
	mod = log -p -1 --name-only --oneline
	show1 = show --pretty="format:" --name-only
```
